export type Styles = {
  'bonus': string;
  'bonus-small': string;
  'reels': string;
  'reels__bonus': string;
};

export type ClassNames = keyof Styles;

declare const styles: Styles;

export default styles;
